# Spring cloud bootstrap. configuration server 연동을 위해 application.yml 파일보다 먼저 읽히게 된다.
# application.yml 과 같은 역할이지만 순서만 다른 것.

spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: ecommerce,gateway

  profiles:
    active: dev
